@{
    ViewBag.Title = "Càmeres";
}

<div class="row">
    @* 10 random images to debug *@
    @for (int i = 0; i < 10; i++)
    {
        <div class="col-sm-6 col-md-4">
            <div class="thumbnail">
                <img src="~/Content/3_germa.jpg" alt="Camera @i">
                <div class="caption">
                    <h3>Camera @i</h3>
                    <p>...</p>
                    <p><a href="#" name="Camera @i" class="btn btn-primary camera-list" role="button">Button</a></p>
                </div>
            </div>
        </div>
    }
</div>

